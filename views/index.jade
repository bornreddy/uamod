extends layout

block content
  h1= ua_letter
  p.date Today is #{date}
  p.time The time is #{time}
  p.sched The schedule is #{ua_schedule}
  p.current current mod: #{current_mod}
  p.next next mod: #{next_mod}
