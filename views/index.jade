extends layout

block content
  h1= I Love you Nishant
  p.date Today is #{date}
  p.time The time is #{time}
  p.sched The schedule is #{ua_schedule}
  p.current current_mod: #{current_mod}
  p.next next_mod: #{next_mod}
